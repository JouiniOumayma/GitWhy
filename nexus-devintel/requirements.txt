# NEXUS-DevIntel — runtime dependencies
# Python >= 3.10 (PEP 604 unions, dataclasses slots).

# Data contracts (Pydantic v2)
pydantic>=2.8,<3

# Neo4j driver: Bolt connection + temporal type conversion
neo4j>=5.24,<6

# PostgreSQL / pgvector access (psycopg 3, not psycopg2)
psycopg[binary]>=3.1,<4

# Reading docker-compose / config files in scripts
PyYAML>=6.0,<7
